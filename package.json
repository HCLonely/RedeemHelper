{
  "name": "redeem-helper",
  "version": "4.1.4",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/update-asf-commands.mjs && node esbuild.config.cjs",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "npm run lint -- --fix",
    "test": "npm run lint && npm run typecheck && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "esbuild": "^0.25.0",
    "eslint": "^10.10.0",
    "globals": "^17.12.0",
    "typescript": "^5.8.0",
    "typescript-eslint": "^8.69.0"
  }
}
